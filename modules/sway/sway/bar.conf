# Bar — disabled: waybar owns the top bar now (swaybar retired)
# The old swaybar/status.sh stack is archived; waybar is exec'd from autostart.conf.
# Keep this file as a stub so `include bar.conf` never fails when toggling.
# To restore swaybar, replace this with the classic bar { status_command ... } block.
# bar {
#     mode dock
#     position top
#     status_command $HOME/.config/sway/status.sh
#     ...
# }
