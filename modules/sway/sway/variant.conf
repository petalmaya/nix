# variant — generated by Nix (see modules/sway/default.nix) when liveSway is off.
# When liveSway is on, this file is the live variant (edit me directly).
# Default is waybar-only (no shell). Noctalia/quickshell variants are injected by Nix at build time.
