exec-once=lucid-qs
# Ensure matugen theme is applied on login even when Lucid owns the wallpaper
exec-once=bash -c 'sleep 1; if [ -f ~/.cache/quickshell/matugen.json ]; then mmsg dispatch reload_config 2>/dev/null || true; fi'
