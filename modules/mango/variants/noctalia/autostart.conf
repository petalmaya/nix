exec-once=noctalia
# Ensure matugen theme is applied on login even when Noctalia owns the wallpaper
exec-once=bash -c 'sleep 1; if [ -f ~/.local/state/nixtop/theme/mango.conf ]; then mmsg dispatch reload_config 2>/dev/null || true; fi'
