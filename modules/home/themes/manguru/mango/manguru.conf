# =============================================================================
# mango config for Alice on Debian
# Ported from modules/home/themes/kurukuru/niri/config.kdl - niri's scrolling
# columns map onto mango's scroller layout, see the per-tag rules below.
#
# Edit freely. Reload with: mmsg dispatch reload_config
# Or bind: Mod+Shift+C (below)
# =============================================================================

# --- Outputs -----------------------------------------------------------
# Adjust monitor names with: wlr-randr
# "name" is a regex - comment out rules you don't have.

monitorrule=name:HDMI-A-1,width:1680,height:1050,x:0,y:0
monitorrule=name:eDP-1,x:1680,y:0

# --- Input ---------------------------------------------------------------

xkb_rules_options=ctrl:nocaps
trackpad_natural_scrolling=1
tap_to_click=1
# touch map-to-output (eDP-1 in the niri config) has no mango equivalent
# found - devicerule doesn't expose an output target, flagging as a gap

# --- Layout --------------------------------------------------------------
# scroller is mango's closest layout to niri's infinite-scrolling columns -
# set as the default for every tag so it behaves like niri out of the box.
# setlayout,tile/setlayout,grid switch a tag to something else on the fly
# if scroller doesn't fit it (see circle_layout note below for cycling).

tagrule=id:1,layout_name:scroller
tagrule=id:2,layout_name:scroller
tagrule=id:3,layout_name:scroller
tagrule=id:4,layout_name:scroller
tagrule=id:5,layout_name:scroller
tagrule=id:6,layout_name:scroller
tagrule=id:7,layout_name:scroller
tagrule=id:8,layout_name:scroller
tagrule=id:9,layout_name:scroller

circle_layout=scroller,tile,grid
# switch_layout normally binds to Mod+N, but Mod+N is mpv below (niri
# parity) - rebind a key to switch_layout by hand if per-tag cycling
# through circle_layout is wanted, it's unbound for now

scroller_structs=20
scroller_default_proportion=0.8
scroller_focus_center=0
scroller_prefer_center=0
edge_scroller_pointer_focus=0
scroller_default_proportion_single=1.0
scroller_proportion_preset=0.333,0.5,0.667,1.0

gappih=6
gappiv=6
gappoh=12
gappov=12

# niri had border{off} + focus-ring{width 3} - mango only has one border
# concept, so this is both at once: a slim colored outline on whichever
# window is focused
borderpx=3
no_border_when_single=0
border_radius=20

# niri's blur{} was global (not per-window), same here - global blur=1
# covers the one per-app windowrule niri had for foot too, mango has no
# opt-in per-window blur, only noblur to opt out
blur=1
blur_optimized=1
blur_params_num_passes=1
blur_params_noise=0.02
blur_params_saturation=1.5
layerrule=noblur:1,layer_name:rexies.notch.quickshell
layerrule=noblur:1,layer_name:rexies.notch.pseudotop
layerrule=noblur:1,layer_name:kurukuru-dock
layerrule=noblur:1,layer_name:kurukuru-volume-osd

# --- Window rules ----------------------------------------------------------

windowrule=appid:^(eog|sxiv|feh|mpv|vlc|file-roller|xarchiver|loupe)$,isfloating:1
windowrule=appid:^org\.gnome\.Nautilus$,title:.*Progress.*,isfloating:1
windowrule=appid:^org\.gnome\.Nautilus$,title:.*Replace.*,isfloating:1

# --- Cursor / misc ---------------------------------------------------------

cursor_theme=capitaine-cursors
cursor_size=24

env=XDG_CURRENT_DESKTOP,mango
env=PYTHONIOENCODING,utf-8

# --- Autostart ---------------------------------------------------------
# dbus-update-activation-environment and the polkit agent need a real
# Nix store path, those two exec-onces live in the small store-built
# stub in default.nix instead, which sources this file.

exec-once=quickshell

# --- Binds -----------------------------------------------------------------

# Terminal / launcher
bind=SUPER,Return,spawn,foot
bind=SUPER,d,spawn,qs ipc call launcher toggle
bind=SUPER+SHIFT,d,spawn,qs ipc call notch toggle
bind=SUPER+SHIFT,s,spawn,qs ipc call notch workspaces
bind=SUPER+SHIFT,x,spawn,qs ipc call notch media


# Screenshot (grim+slurp - mango has no built-in screenshot dispatch).
# spawn's comma-separated fields get re-joined and word-split by wordexp
# before exec, so the whole script has to be one field, single-quoted,
# or the internal spaces would get sliced into separate argv words
bind=SUPER,Print,spawn,sh -c 'grim -g "$(slurp)" ~/Pictures/$(date +%Y-%m-%d_%H-%M-%S).png'
bind=SUPER,p,spawn,qs ipc call notifications,clear

# Lock via the quickshell lockscreen
bind=SUPER,l,spawn,qs ipc call lockscreen lock

# Apps
bind=SUPER,w,spawn,firefox-esr
bind=SUPER,e,spawn,nautilus
bind=SUPER,a,spawn,foot -e yazi
bind=SUPER,m,spawn,spotify
bind=SUPER,n,spawn,mpv --player-operation-mode=pseudo-gui

# Audio / brightness
bind=none,XF86AudioRaiseVolume,spawn,pactl,set-sink-volume,@DEFAULT_SINK@,+5%
bind=none,XF86AudioLowerVolume,spawn,pactl,set-sink-volume,@DEFAULT_SINK@,-5%
bind=none,XF86AudioMute,spawn,pactl,set-sink-mute,@DEFAULT_SINK@,toggle
bind=none,XF86MonBrightnessUp,spawn,brightnessctl,set,+5%
bind=none,XF86MonBrightnessDown,spawn,brightnessctl,set,5%-

# Window management
bind=SUPER,c,killclient
bind=ALT,F4,killclient
bind=SUPER,f,togglemaximizescreen
bind=SUPER+SHIFT,f,togglefullscreen
bind=SUPER,space,togglefloating

# Focus (sway-like arrows) - focusdir is spatial, works the same in
# scroller as niri's column/window focus
bind=SUPER,Left,focusdir,left
bind=SUPER,Right,focusdir,right
bind=SUPER,Up,focusdir,up
bind=SUPER,Down,focusdir,down
bind=SUPER,KP_Left,focusdir,left
bind=SUPER,KP_Right,focusdir,right
bind=SUPER,KP_Up,focusdir,up
bind=SUPER,KP_Down,focusdir,down

# Move
bind=SUPER+SHIFT,Left,exchange_client,left
bind=SUPER+SHIFT,Right,exchange_client,right
bind=SUPER+SHIFT,Up,exchange_client,up
bind=SUPER+SHIFT,Down,exchange_client,down
bind=SUPER+SHIFT,KP_Left,exchange_client,left
bind=SUPER+SHIFT,KP_Right,exchange_client,right
bind=SUPER+SHIFT,KP_Up,exchange_client,up
bind=SUPER+SHIFT,KP_Down,exchange_client,down

# Column consume / expel - approximate, scroller_stack folds a window
# into/out of the neighbouring column rather than niri's exact model
bind=SUPER,h,scroller_stack,left
bind=SUPER,v,scroller_stack,right
# niri's consume-or-expel-left/right (Mod+BracketLeft/Right) has no
# single-bind mango equivalent beyond the above - left unbound

# Workspaces (tags)
bind=SUPER+CTRL,Right,viewtoright,0
bind=SUPER+CTRL,Left,viewtoleft,0
bind=SUPER+CTRL+SHIFT,Right,tagtoright,0
bind=SUPER+CTRL+SHIFT,Left,tagtoleft,0
bind=SUPER+CTRL,KP_Right,viewtoright,0
bind=SUPER+CTRL,KP_Left,viewtoleft,0
bind=SUPER+CTRL+SHIFT,KP_Right,tagtoright,0
bind=SUPER+CTRL+SHIFT,KP_Left,tagtoleft,0
bind=SUPER+CTRL,Page_Down,tagtoright,0
bind=SUPER+CTRL,Page_Up,tagtoleft,0
bind=SUPER,1,view,1,0
bind=SUPER,2,view,2,0
bind=SUPER,3,view,3,0
bind=SUPER,4,view,4,0
bind=SUPER,5,view,5,0
bind=SUPER,6,view,6,0
bind=SUPER+SHIFT,1,tag,1,0
bind=SUPER+SHIFT,2,tag,2,0
bind=SUPER+SHIFT,3,tag,3,0
bind=SUPER+SHIFT,4,tag,4,0
bind=SUPER+SHIFT,5,tag,5,0
bind=SUPER+SHIFT,6,tag,6,0

# Column sizing - set_proportion/switch_proportion_preset are the closest
# mango has, there's no relative +/-10% resize for scroller like niri's
# set-column-width, so Mod+Minus/Mod+Equal are unbound (gap)
bind=SUPER,r,switch_proportion_preset,
bind=ALT,e,set_proportion,1.0

# Quit / reload / overview
bind=SUPER+SHIFT,q,quit
bind=SUPER+SHIFT,c,reload_config
bind=SUPER,s,toggleoverview
# Alt-Tab-style window switcher
bind=ALT,Tab,toggleoverview
bind=SUPER,Tab,togglejump

# Mouse drag move/resize - niri does this by default, mango needs it
# spelled out
mousebind=SUPER,btn_left,moveresize,curmove
mousebind=SUPER,btn_right,moveresize,curresize

# Dynamic colour template written by matugen
source=~/.config/mango/flutterice.conf
